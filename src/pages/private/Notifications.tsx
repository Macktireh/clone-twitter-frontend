import * as React from "react";

import Layout from "@/layout/Layout";

const Notifications: React.FC = () => {
  return (
    <Layout>
      <div>Notifications works !!</div>
    </Layout>
  );
};

export default Notifications;
