import * as React from "react";

import Layout from "@/layout/Layout";

const Lists: React.FC = () => {
  return (
    <Layout>
      <div>Lists works !!</div>
    </Layout>
  );
};

export default Lists;
